import { ImagePathUrlPipe } from './image-path-url.pipe';

describe('ImagePathUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagePathUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
